{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Lightweight API gateway with routing, rate limiting, auth and middleware",
  "main": "src/gateway.js",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
